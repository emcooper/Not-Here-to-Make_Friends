<nav class="navbar navbar-expand-lg">
  <div class="container">
  <a class="navbar-brand" href="/">Not Here to Make Friends</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
  <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Your Leagues</a>
  <div class="dropdown-menu">
    <% current_user.active_leagues.each do |league| %>
      <p><%= link_to league.name, league_path(league), class: "dropdown-item" %></p>
    <% end %>
    <% if current_season.draft_date && Time.now < current_season.draft_date %>
      <p><%= link_to "+ Create or Join", start_path, class: "dropdown-item green-text" %></p>
    <% end %>
  </div>
</li>
      <li class="nav-item">
        <%= link_to "This Week", (week_path(current_week) if current_week) || "#", class: "nav-link" %>
      </li>
      <% if current_admin? %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Admin Tasks</a>
        <div class="dropdown-menu">
            <p><%= link_to "Draft", admin_draft_path, class: "dropdown-item" %></p>
        </div>
      </li>
      <% end %>
      <li class="nav-item">
        <%= link_to "Log Out", logout_path, method: :delete, class: "nav-link" %>
      </li>
    </ul>
  </div>
</div>
</nav>
<div class="container">
